# LOOKY 网页启动、PWA 与带宽优化方案

## 一、目标

在不牺牲功能、不影响后台调用、不修改 iOS `100dvh`、固定 body 和键盘适配代码的前提下，解决以下问题：

1. iOS 浏览器能打开，但添加到主屏幕后白屏、转圈或无法进入。
2. 新版本发布时，用户不能被卡在更新流程中，也不能因为更新丢失本地数据。
3. 论坛、音乐、视频、生活等模块按需加载时，点击页面仍然能够正常出现。
4. 降低首页重复下载、图片/音频下载和静态资源重复请求造成的 Netlify 带宽消耗。

## 二、当前项目已经确认的风险

### 1. 首页和静态资源偏大

目前项目中比较大的文件包括：

| 文件 | 当前体积（约） |
| --- | ---: |
| `index.html` | 1.41 MB |
| `style/main.css` | 1.80 MB |
| `js/features/chat-service.js` | 1.29 MB |
| `js/features/forum.js` | 0.80 MB |
| `images/ticket-bg.png` | 2.17 MB |
| `assets/silence.mp3` | 3.49 MB |
| `assets/ringtone.mp3` | 1.83 MB |

首页包含大量页面的完整 HTML。项目中有约 131 个 `<img>`，只有很少的图片设置了懒加载。即使某些模块暂时没有显示，HTML 中的图片、静态模块和样式仍可能触发下载。

### 2. Service Worker 当前没有真正预缓存

`service-worker.js` 中虽然定义了 `CRITICAL_URLS`，但当前安装阶段只执行 `skipWaiting()`，没有使用这些地址执行预缓存。

同时，文档、脚本和样式使用了 Network First，并通过 `cache: 'no-cache'` 请求网络。这会让主屏幕应用在冷启动或网络不稳定时等待网络；如果请求失败，缓存又可能没有可用的完整资源。

### 3. 更新可能造成版本混用

当前 Service Worker 在安装时直接 `skipWaiting()`，激活时又执行 `clients.claim()`。如果新版本接管了仍在运行的旧页面，旧页面后续请求可能由新 Service Worker 处理，容易产生旧 HTML、新 JS 或旧 JS、新资源混用的问题。

更新提示还在主应用完成启动前显示。对 iOS 主屏幕应用来说，这可能让用户误以为应用没有打开。

### 4. iOS 主屏幕应用不是 Safari 的同一个运行上下文

当前登录信息使用 `localStorage`，业务数据使用 IndexedDB。iOS 主屏幕 Web App 与 Safari 的数据环境是隔离的，因此不能假设用户在 Safari 中的登录状态会自动出现在主屏幕应用中。

## 三、最重要的更新原则

### 原则 A：新版本先准备，准备好后再接管

更新流程必须采用“两阶段更新”：

1. 新 Service Worker 在后台下载并校验新版本的核心资源。
2. 只要有一个核心资源下载失败，新版本就保持等待，不接管当前页面。
3. 当前用户继续使用旧版本，不弹出强制更新，不强制刷新。
4. 新版本准备完整后，只显示非阻塞提示，例如“新版本已准备好，下次刷新生效”。
5. 用户主动刷新、重新打开应用，或者确认没有未保存操作后，再切换到新版本。

这样即使发布时网络中断，用户也不会被卡在半更新状态。

### 原则 B：更新提示不能参与启动链路

启动顺序应当是：

```text
显示启动壳
  ↓
尽快显示登录页或首页骨架
  ↓
读取本地数据
  ↓
显示完整页面
  ↓
空闲时检查更新、预加载非核心模块
```

更新检查、版本日志、统计脚本和非核心功能都不能阻塞首页显示。

### 原则 C：页面先出现，模块再准备

论坛、音乐、视频、生活等页面点击后应立即显示页面外壳或骨架，然后由模块完成初始化：

```text
点击页面
  ↓
立即显示页面标题、导航和骨架
  ↓
ensureModuleReady()
  ↓
成功：填充内容
失败：显示重试按钮，不影响其他页面
```

每个模块的 `ensureModuleReady()` 必须是可重复调用、只初始化一次的 Promise。这样既可以按需加载，也不会因为重复点击而重复初始化或重复调用后台接口。

## 四、实施步骤

### 第 0 步：先加启动诊断，不改变功能

记录以下启动阶段，但不要把日志页面或诊断逻辑放进关键启动路径：

- `main module loaded`
- `DOMContentLoaded entered`
- Service Worker 是否接管
- IndexedDB 打开是否成功
- 本地数据读取耗时
- `showHomeScreen` 是否执行
- 最后一个启动错误

如果启动超过 6～8 秒，应显示“启动较慢 / 刷新重试 / 回到登录页”，不能继续显示纯白屏。

### 第 1 步：先修 Service Worker，优先保证不被更新卡住

建议的缓存分层：

#### A. 版本化应用缓存

每次部署使用独立的缓存名称，例如：

```text
looky-app-2026.09.12
```

缓存中只放当前版本已经确认存在的核心资源。新缓存完整安装成功前，不删除旧缓存。

#### B. 请求策略

| 资源 | 策略 |
| --- | --- |
| `index.html` | 网络优先，但设置短超时；网络失败立即回退旧缓存 |
| 版本化 CSS/JS | 当前版本缓存优先，缺失时走网络 |
| 图片/音频 | 使用时缓存，不在启动阶段全部下载 |
| 登录、认证、API、MCP | 网络请求，不缓存业务响应 |
| `version.json` | 后台请求，不阻塞启动 |
| POST/PUT/DELETE | 不交给缓存处理 |

核心资源的网络请求需要超时。例如 2～3 秒没有拿到新版本资源，就继续使用旧缓存，不要一直等待。

#### C. 不再自动接管正在运行的旧页面

建议取消“安装后立即接管当前页面”的默认行为，或者改为只在用户确认后发送 `SKIP_WAITING`。当前页面继续使用旧版本，下一次刷新再使用新版本。

#### D. 防止核心资源清单失效

`CRITICAL_URLS` 不能长期手动维护到遗漏。应根据 `main.js` 的静态 import 生成或定期校验资源清单；清单中任何核心文件不存在时，新 Service Worker 安装失败并继续使用旧版本。

### 第 2 步：修首页重复带宽，不改变页面功能

按风险从低到高处理：

1. 把非首页图片设置为 `loading="lazy"` 和 `decoding="async"`。
2. 对隐藏页面中的图片使用 `data-src`，进入对应页面时再赋值给 `src`。
3. 将大 PNG/JPG 转为 WebP 或 AVIF；先处理 `ticket-bg.png`、`dream-card.png`、地图和车辆图片。
4. 音频统一使用 `preload="none"`，只有用户点击播放时才设置或加载音频源。
5. 视频不设置自动下载，使用 `preload="metadata"` 或 `none`。

这些改动只影响资源何时下载，不会改变页面的后台调用，也不会删除用户功能。

### 第 3 步：拆分非核心 JS，但保留点击兜底

第一阶段不要一次性重构所有模块，只处理明显较大的模块：

- 论坛
- 音乐
- 生活
- 视频通话
- 图库 AI

核心登录、首页、聊天基础界面和数据库访问先保持稳定。非核心模块在首页首屏完成后利用浏览器空闲时间预取；如果用户马上点击，页面先显示骨架，再等待同一个模块 Promise。

不能使用“点击后才开始下载、点击前什么都不准备”的简单懒加载，否则会把首页卡顿转移到后续页面。

### 第 4 步：稳定 PWA 入口

在不修改 iOS 布局和键盘代码的前提下，检查并稳定：

- `manifest.json` 的 `start_url` 使用根路径 `/`。
- 增加明确的 `scope` 和 `id`，避免安装入口变化。
- 确认 `looky12138.com` 已在 Pages 的 Custom domains 中正式绑定。
- 发布更新后，测试删除旧主屏幕图标再重新添加，排除旧 manifest 和旧 Service Worker 缓存。
- 主屏幕第一次打开没有 Safari 的登录数据时，应正常显示登录页，而不是假设已有登录状态。

如果需要让 Safari 和主屏幕应用共享登录状态，必须设计安全的服务端 Cookie 或一次性登录交接流程，不能直接复制密码或把本地令牌暴露到 URL。

## 五、更新发布流程

每次发布按照以下顺序执行：

1. 保留上一版可回滚部署。
2. 在预览地址测试浏览器和 iOS 主屏幕启动。
3. 检查首页、登录、聊天、论坛、音乐、视频、生活和本地数据读取。
4. 发布新版本，但不强制刷新在线用户。
5. 观察新 Service Worker 是否完整安装。
6. 只有核心资源全部准备完成后，才允许下次启动使用新版本。
7. 如果出现错误，回滚部署即可；旧 Service Worker 仍能继续服务旧版本。

## 六、验收标准

### 启动

- 浏览器冷启动可以进入。
- iOS 主屏幕冷启动可以进入。
- 弱网时不出现无限白屏。
- IndexedDB 读取失败时有可操作提示，不覆盖旧数据。
- 没有登录状态时显示登录页，而不是空白。

### 更新

- 用户正在聊天或编辑内容时不会被强制刷新。
- 新版本资源下载失败时，用户仍能继续使用旧版本。
- 新版本不会出现旧 HTML 与新 JS 混用。
- 更新后本地 IndexedDB 数据、登录流程和页面功能不丢失。

### 页面和调用

- 点击任何模块时立即出现页面外壳或骨架。
- 模块加载失败时有重试入口。
- 每个模块只初始化一次。
- 不重复发送已有的后台请求。
- 论坛、音乐、视频、生活的首次进入和再次进入都能正常工作。

### 带宽

- 首屏不再下载非首屏图片、音频和视频。
- 重复启动优先使用已准备好的版本缓存。
- Netlify 的网络带宽和请求量分别观察，不把本地 IndexedDB 数据误认为网站带宽。
- 每次只做一类改动，确认功能无回归后再继续下一步。

## 七、明确不做的事情

- 不直接删除 Service Worker 或清空用户缓存来“解决”问题。
- 不把所有页面一次性改成点击后才加载。
- 不缓存登录响应、聊天响应或其他个性化业务数据。
- 不修改 iOS `100dvh`、固定 body、键盘适配和输入框逻辑。
- 不因为 Netlify 账单高就直接升级套餐；先减少重复传输，再重新评估套餐。
- 不把 Cloudflare 橙云、Netlify 迁移和前端代码重构放在同一次发布中。

## 八、推荐落地顺序

```text
启动诊断
  → Service Worker 两阶段更新
  → 启动失败兜底
  → 非首屏图片与音频优化
  → 大模块按需加载 + 空闲预取
  → 重新观察带宽和 iOS 主屏幕表现
  → 再决定是否迁移或升级 Netlify
```

这套顺序的核心是先保证“旧版本可用、新版本后台准备”，再做体积和按需加载优化。任何一步出现问题，都可以只回滚这一层，不影响整个网站。
